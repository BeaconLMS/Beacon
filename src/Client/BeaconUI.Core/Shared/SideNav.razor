<div class="nav-scrollable">
    <nav class="d-flex flex-column">
        <CurrentUserMemberships Context="Memberships">
            @foreach (var membership in Memberships)
            {
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="@NavigationHelper.GetLabDetailsHref(membership.LaboratoryId)" Match="NavLinkMatch.All">
                        <span class="icon" aria-hidden="true">
                            <i class="fa-regular fa-flask"></i>
                        </span>
                        <span>@membership.LaboratoryName</span>
                    </NavLink>
                </div>
            }
        </CurrentUserMemberships>
        <div class="nav-item px-3">
            <a href="new-lab">New laboratory</a>
        </div>
    </nav>
</div>
